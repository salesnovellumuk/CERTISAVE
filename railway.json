{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && DJANGO_SUPERUSER_PASSWORD=$SUPERUSER_PASSWORD python manage.py createsuperuser --noinput --username=$SUPERUSER_USERNAME --email=$SUPERUSER_EMAIL && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}